{
    "version": "1.0",
    "name": "Ensurity Mobile",
    "description": "Receives Ensurity Mobile vehicle telemetry via webhook and on a schedule and posts vehicle positions to the map.",
    "compute": {
        "memory": 512,
        "timeout": 60
    },
    "permissions": [
        {
            "resource": "feature:*",
            "required": true,
            "description": "Ensurity Mobile posts vehicle positions to the map"
        }
    ],
    "invocations": {
        "incoming": {
            "schedule": {
                "description": "Polls Ensurity Mobile for updated vehicle telemetry on a schedule.",
                "default": {
                    "enabled": true,
                    "schedule": "rate(1 minute)"
                }
            },
            "webhook": {
                "description": "Receives Ensurity Mobile vehicle telemetry pushed to the webhook endpoint.",
                "default": {
                    "enabled": true
                }
            }
        }
    }
}
